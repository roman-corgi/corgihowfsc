name: corgiloop
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - unzip
  # Common scientific packages that might be needed
  #  - numpy
  #  - scipy
  #  - matplotlib
  #  - astropy
  - pip:
    - git+https://github.com/roman-corgi/cgi-howfsc.git
    - git+https://github.com/nasa-jpl/cgi-eetc.git